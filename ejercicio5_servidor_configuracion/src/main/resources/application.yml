server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ext-gonbonan/ejercicio5_cloud_microservicios
          default-label: main

  devtools:
    restart:
      enabled: true

logging:
  level:
    '[org.springframework.cloud.config]': DEBUG
    '[org.springframework.boot.autoconfigure.jdbc]': DEBUG